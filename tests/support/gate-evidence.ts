/**
 * Test compatibility export. The evidence schema and validator live in the
 * production finalization package so tests exercise the shipping code.
 */
export * from "../../scripts/switchable-baseline/gate-evidence";
