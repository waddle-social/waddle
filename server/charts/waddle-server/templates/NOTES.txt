Waddle Social server has been {{ if .Release.IsInstall }}installed{{ else }}upgraded{{ end }}.

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Chart:   {{ .Chart.Name }}-{{ .Chart.Version }}
{{ if .Values.image.digest -}}
Image:   {{ .Values.image.repository }}@{{ .Values.image.digest }}
{{- else -}}
Image:   {{ .Values.image.repository }}:{{ default .Chart.AppVersion .Values.image.tag }}
{{- end }}

Runtime secrets are operator-managed. Ensure these env vars are provided by
secret values, secret.existingSecret, or secret.runtimeSecretName:

  WADDLE_SESSION_KEY
  WADDLE_OCCUPANT_ID_SECRET

Do not rotate WADDLE_OCCUPANT_ID_SECRET without an explicit migration plan;
rotation breaks XEP-0421 occupant-id continuity for existing room/user pairs.
{{- if not .Values.deployment.uuid }}

WARNING: deployment.uuid is not set. Pods against a durable (non in-memory)
database stay permanently NOT READY until the database lineage is enrolled:
set deployment.uuid to a stable UUID and run a one-shot
deployment.lineageAction=enroll rollout. See docs/operations/db-lineage.md.
{{- end }}
{{- if .Values.deployment.lineageAction }}

REMINDER: deployment.lineageAction is set ("{{ .Values.deployment.lineageAction }}").
This is a ONE-SHOT operator action — remove it in the next upgrade once the
fleet is ready; leaving it rendered re-applies it on every pod restart.
{{- end }}

{{- if .Values.secret.existingSecret }}

Using operator-managed Secret: {{ .Values.secret.existingSecret }}
{{- end }}

To check pod status:
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}

To follow server logs:
  kubectl -n {{ .Release.Namespace }} logs -f -l app.kubernetes.io/instance={{ .Release.Name }}
