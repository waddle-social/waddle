[package]
name = "decision-polls"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
chrono = { version = "0.4.44", default-features = false, features = ["clock", "std"] }
sha2 = "0.11"
wit-bindgen = "0.57.1"
