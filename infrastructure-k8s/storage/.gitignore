# Proxmox CSI Storage - Sensitive Files
# ======================================
#
# This .gitignore prevents accidental commit of credential files.
# Template files (like proxmox-credentials-secret.yaml) are tracked
# but should NEVER contain real credentials.
#
# For production, use:
# - sealed-secrets: Encrypt secrets client-side
# - external-secrets: Fetch from Vault, AWS SM, etc.
# - sops: Mozilla SOPS with age/gpg encryption

# =============================================================================
# Actual credential files (NEVER commit these)
# =============================================================================

# Proxmox CSI config with real credentials
proxmox-csi-config.yaml
proxmox-config.yaml
config.yaml

# Any file with .secret suffix
*.secret.yaml
*.secret.yml
*.secrets.yaml
*.secrets.yml

# =============================================================================
# Environment files
# =============================================================================

.env
.env.local
.env.*.local

# =============================================================================
# Keys and certificates
# =============================================================================

*.key
*.pem
*.crt
*.p12
*.pfx

# =============================================================================
# Backup and temporary files
# =============================================================================

*.bak
*.backup
*~
*.swp
*.swo
.DS_Store

# =============================================================================
# Terraform state (if accidentally placed here)
# =============================================================================

*.tfstate
*.tfstate.*
.terraform/
