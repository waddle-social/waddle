# cert-manager - Sensitive Files
# ==============================
#
# This .gitignore prevents accidental commit of credential files.
# Template files (like cloudflare-api-token-secret.yaml) are tracked
# but should NEVER contain real credentials.
#
# For production, use:
# - sealed-secrets: Encrypt secrets client-side
# - external-secrets: Fetch from Vault, AWS SM, etc.
# - sops: Mozilla SOPS with age/gpg encryption

# =============================================================================
# Cloudflare API token (NEVER commit these)
# =============================================================================

cloudflare-api-token.txt
cloudflare-token.yaml
cloudflare-*.secret.yaml
api-token.txt

# =============================================================================
# Local test certificates (NEVER commit these)
# =============================================================================

*.crt
*.key
*.pem
*.p12
*.pfx
*.csr

# =============================================================================
# Secret files with real credentials
# =============================================================================

*.secret.yaml
*.secret.yml
*.secrets.yaml
*.secrets.yml

# =============================================================================
# Environment files
# =============================================================================

.env
.env.local
.env.*.local

# =============================================================================
# Backup and temporary files
# =============================================================================

*.bak
*.backup
*~
*.swp
*.swo
*.tmp
.DS_Store

# =============================================================================
# Terraform state (if accidentally placed here)
# =============================================================================

*.tfstate
*.tfstate.*
.terraform/
