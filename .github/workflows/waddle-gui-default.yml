# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: waddle-gui-default
on:
  push:
    branches:
    - main
    paths:
    - .github/workflows/waddle-gui-default.yml
    - app/gui/bun.lock
    - app/gui/dist/**
    - app/gui/env.cue
    - app/gui/index.html
    - app/gui/package.json
    - app/gui/public/**
    - app/gui/schema/**
    - app/gui/src/**
    - app/gui/tsconfig*.json
    - app/gui/vite.config.ts
    - app/gui/wrangler.jsonc
    - cue.mod/**
  workflow_dispatch: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
jobs:
  waddle-gui-default:
    name: waddle-gui-default
    runs-on: ubuntu-latest
    environment: production
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Setup cuenv (release)
      run: curl -sSL -o /usr/local/bin/cuenv https://github.com/cuenv/cuenv/releases/latest/download/cuenv-linux-x64 && chmod +x /usr/local/bin/cuenv && /usr/local/bin/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup 1Password
      run: cuenv exec -- cuenv secrets setup onepassword
      env:
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    - name: 'Run pipeline: default'
      run: cuenv ci --pipeline default --path app/gui
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        CUENV_ENVIRONMENT: production
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
