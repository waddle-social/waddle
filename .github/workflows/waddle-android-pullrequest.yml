# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: waddle-android-pullRequest
on:
  pull_request:
    paths:
    - .github/workflows/waddle-android-*.yml
    - .github/workflows/waddle-android-pullrequest.yml
    - apps/android/app/**
    - apps/android/build.gradle.kts
    - apps/android/build.gradle.kts/**
    - apps/android/core/client/build.gradle.kts
    - apps/android/core/client/build.gradle.kts/**
    - apps/android/core/client/consumer-rules.pro
    - apps/android/core/client/consumer-rules.pro/**
    - apps/android/core/client/lint.xml
    - apps/android/core/client/lint.xml/**
    - apps/android/core/client/src/**
    - apps/android/core/client/src/main/kotlin/social/waddle/client/ffi/**
    - apps/android/debug.keystore
    - apps/android/debug.keystore/**
    - apps/android/detekt.yml
    - apps/android/detekt.yml/**
    - apps/android/env.cue
    - apps/android/env.cue/**
    - apps/android/gradle.properties
    - apps/android/gradle.properties/**
    - apps/android/gradle/**
    - apps/android/gradlew
    - apps/android/gradlew/**
    - apps/android/schema/**
    - apps/android/settings.gradle.kts
    - apps/android/settings.gradle.kts/**
    - cue.mod/**
    - env.cue
    - env.cue/**
    - flake.lock
    - flake.lock/**
    - flake.nix
    - flake.nix/**
    - nix/patches/cuenv-0.54.0-fail-closed-discovery.patch
    - nix/patches/cuenv-0.54.0-fail-closed-discovery.patch/**
    - scripts/build-android-rust.sh
    - scripts/build-android-rust.sh/**
    - scripts/check-android-ffi-bindings.sh
    - scripts/check-android-ffi-bindings.sh/**
    - scripts/setup-android-sdk.sh
    - scripts/setup-android-sdk.sh/**
    - server/Cargo.lock
    - server/Cargo.lock/**
    - server/Cargo.toml
    - server/Cargo.toml/**
    - server/crates/waddle-xmpp-client-ffi/**
    - server/crates/waddle-xmpp-client/**
    - server/rust-toolchain.toml
    - server/rust-toolchain.toml/**
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
  packages: write
jobs:
  waddle-android-pullRequest:
    name: waddle-android-pullRequest
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Cache Android SDK and Gradle on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        path: |-
          /home/runner/.android-sdk
          /home/runner/.gradle
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Build cuenv (nix)
      run: |-
        . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh
        nix build .#cuenv -L --accept-flake-config
        echo "$(pwd)/result/bin" >> "$GITHUB_PATH" 2>/dev/null || echo "$(pwd)/result/bin" >> "$BUILDKITE_ENV_FILE" 2>/dev/null || true
        ./result/bin/cuenv sync ci
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: 'Run pipeline: pullRequest'
      run: cuenv ci --pipeline pullRequest --path apps/android
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
