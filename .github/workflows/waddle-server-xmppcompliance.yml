# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: waddle-server-xmppCompliance
on:
  push:
    branches:
    - main
    paths:
    - .github/workflows/waddle-server-xmppcompliance.yml
    - cue.mod/**
    - flake.lock
    - flake.lock/**
    - flake.nix
    - flake.nix/**
    - nix/patches/cuenv-0.54.0-fail-closed-discovery.patch
    - nix/patches/cuenv-0.54.0-fail-closed-discovery.patch/**
    - server/.config/nextest.toml
    - server/.config/nextest.toml/**
    - server/Cargo.lock
    - server/Cargo.lock/**
    - server/Cargo.toml
    - server/Cargo.toml/**
    - server/charts/waddle-server/**
    - server/crates/**
    - server/env.cue
    - server/extensions/**
    - server/rust-toolchain.toml
    - server/rust-toolchain.toml/**
    - server/schema/**
    - server/wit/**
  pull_request:
    branches:
    - main
    paths:
    - .github/workflows/waddle-server-xmppcompliance.yml
    - cue.mod/**
    - flake.lock
    - flake.lock/**
    - flake.nix
    - flake.nix/**
    - nix/patches/cuenv-0.54.0-fail-closed-discovery.patch
    - nix/patches/cuenv-0.54.0-fail-closed-discovery.patch/**
    - server/.config/nextest.toml
    - server/.config/nextest.toml/**
    - server/Cargo.lock
    - server/Cargo.lock/**
    - server/Cargo.toml
    - server/Cargo.toml/**
    - server/charts/waddle-server/**
    - server/crates/**
    - server/env.cue
    - server/extensions/**
    - server/rust-toolchain.toml
    - server/rust-toolchain.toml/**
    - server/schema/**
    - server/wit/**
  workflow_dispatch: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: none
  packages: read
  id-token: write
jobs:
  nixXmppServerTests:
    name: nixXmppServerTests
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: nixXmppServerTests
      run: nix build --print-build-logs ../#checks.x86_64-linux.waddle-server-xmpp-server-tests
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  nixXmppUnitTests:
    name: nixXmppUnitTests
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: nixXmppUnitTests
      run: nix build --print-build-logs ../#checks.x86_64-linux.waddle-server-xmpp-unit-tests
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  nixXmppXepIntegration:
    name: nixXmppXepIntegration
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: nixXmppXepIntegration
      run: nix build --print-build-logs ../#checks.x86_64-linux.waddle-server-xmpp-xep-integration
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
