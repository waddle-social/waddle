# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: waddle-server-xmppCompliance
on:
  push:
    branches:
    - main
    paths:
    - .github/workflows/waddle-server-xmppcompliance.yml
    - cue.mod/**
    - server/../flake.lock
    - server/../flake.nix
    - server/Cargo.lock
    - server/Cargo.toml
    - server/crates/**
    - server/env.cue
    - server/schema/**
    - server/wit/**
  workflow_dispatch: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
  id-token: write
jobs:
  nixXmppServerTests:
    name: nixXmppServerTests
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        fetch-depth: 2
    - name: Set up Namespace Nix cache
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Install Determinate Nix
      uses: DeterminateSystems/determinate-nix-action@92ffb5400c3776307a27a1727d7e2ac3dcd9f844
      with:
        extra-conf: accept-flake-config = true
    - name: Setup FlakeHub Cache
      uses: DeterminateSystems/flakehub-cache-action@e134de896b2302c1584a7b54ff35432708607d44
      with:
        flakehub-flake-name: waddle-social/waddle
        use-gha-cache: disabled
    - name: nixXmppServerTests
      run: nix build ../#checks.x86_64-linux.waddle-server-xmpp-server-tests -L --accept-flake-config
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  nixXmppUnitTests:
    name: nixXmppUnitTests
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        fetch-depth: 2
    - name: Set up Namespace Nix cache
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Install Determinate Nix
      uses: DeterminateSystems/determinate-nix-action@92ffb5400c3776307a27a1727d7e2ac3dcd9f844
      with:
        extra-conf: accept-flake-config = true
    - name: Setup FlakeHub Cache
      uses: DeterminateSystems/flakehub-cache-action@e134de896b2302c1584a7b54ff35432708607d44
      with:
        flakehub-flake-name: waddle-social/waddle
        use-gha-cache: disabled
    - name: nixXmppUnitTests
      run: nix build ../#checks.x86_64-linux.waddle-server-xmpp-unit-tests -L --accept-flake-config
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  nixXmppXepIntegration:
    name: nixXmppXepIntegration
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        fetch-depth: 2
    - name: Set up Namespace Nix cache
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Install Determinate Nix
      uses: DeterminateSystems/determinate-nix-action@92ffb5400c3776307a27a1727d7e2ac3dcd9f844
      with:
        extra-conf: accept-flake-config = true
    - name: Setup FlakeHub Cache
      uses: DeterminateSystems/flakehub-cache-action@e134de896b2302c1584a7b54ff35432708607d44
      with:
        flakehub-flake-name: waddle-social/waddle
        use-gha-cache: disabled
    - name: nixXmppXepIntegration
      run: nix build ../#checks.x86_64-linux.waddle-server-xmpp-xep-integration -L --accept-flake-config
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
