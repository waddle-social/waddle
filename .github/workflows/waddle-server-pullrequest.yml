# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: waddle-server-pullRequest
on:
  pull_request:
    paths:
    - .github/workflows/waddle-android-*.yml
    - .github/workflows/waddle-chat-*.yml
    - .github/workflows/waddle-cloud-*.yml
    - .github/workflows/waddle-colony-*.yml
    - .github/workflows/waddle-server-*.yml
    - .github/workflows/waddle-server-pullrequest.yml
    - .github/workflows/waddle-website-*.yml
    - .gitignore
    - .gitignore/**
    - .rules.cue
    - .rules.cue/**
    - apps/android/env.cue
    - apps/android/env.cue/**
    - apps/apple/**/Tests/**
    - apps/apple/Waddle/RustClient/Generated/**
    - chat/env.cue
    - chat/env.cue/**
    - chat/tests/**
    - colony/env.cue
    - colony/env.cue/**
    - cue.mod/**
    - cuenv.lock
    - cuenv.lock/**
    - docs/evidence/**
    - docs/planning/switchable-alternative.md
    - docs/planning/switchable-alternative.md/**
    - docs/product/critical-journeys.json
    - docs/product/critical-journeys.json/**
    - docs/product/gate-evidence.json
    - docs/product/gate-evidence.json/**
    - docs/product/performance-profile.json
    - docs/product/performance-profile.json/**
    - env.cue
    - env.cue/**
    - flake.lock
    - flake.lock/**
    - flake.nix
    - flake.nix/**
    - infrastructure/waddle.cloud/env.cue
    - infrastructure/waddle.cloud/env.cue/**
    - infrastructure/waddle.cloud/gitops/kustomization-infra-waddle-server.yaml
    - infrastructure/waddle.cloud/gitops/kustomization-infra-waddle-server.yaml/**
    - infrastructure/waddle.cloud/gitops/waddle-server-source.yaml
    - infrastructure/waddle.cloud/gitops/waddle-server-source.yaml/**
    - infrastructure/waddle.cloud/gitops/waddle-server/**
    - nix/patches/cuenv-0.54.0-fail-closed-discovery.patch
    - nix/patches/cuenv-0.54.0-fail-closed-discovery.patch/**
    - server/**/env.cue
    - server/.config/nextest.toml
    - server/.config/nextest.toml/**
    - server/Cargo.lock
    - server/Cargo.lock/**
    - server/Cargo.toml
    - server/Cargo.toml/**
    - server/capabilities.toml
    - server/capabilities.toml/**
    - server/charts/waddle-server/**
    - server/crates/**
    - server/crates/**/tests/**
    - server/crates/waddle-server/src/server/routes/websocket/tests/**
    - server/crates/waddle-server/tests/server_capability_manifest.rs
    - server/crates/waddle-server/tests/server_capability_manifest.rs/**
    - server/deployment.cue
    - server/deployment.cue/**
    - server/env.cue
    - server/env.cue/**
    - server/extensions/**
    - server/rust-toolchain.toml
    - server/rust-toolchain.toml/**
    - server/schema/**
    - server/scripts/**
    - server/wit/**
    - tests/**
    - website/env.cue
    - website/env.cue/**
    - xeps/xep-*.xml
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: none
  packages: read
  id-token: write
jobs:
  build-cuenv:
    name: build.cuenv
    runs-on: namespace-profile-linux-x86
    timeout-minutes: 30
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Build cuenv (nix)
      run: |-
        . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh
        nix build .#cuenv -L --accept-flake-config
        echo "$(pwd)/result/bin" >> "$GITHUB_PATH" 2>/dev/null || echo "$(pwd)/result/bin" >> "$BUILDKITE_ENV_FILE" 2>/dev/null || true
        ./result/bin/cuenv sync ci
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Upload cuenv
      uses: actions/upload-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: result/bin/cuenv
        if-no-files-found: error
  checkCiDrift:
    name: checkCiDrift
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: ${{ runner.temp }}/cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x "$RUNNER_TEMP/cuenv-bootstrap/cuenv"
        echo "$RUNNER_TEMP/cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: checkCiDrift
      run: cuenv task checkCiDrift --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  checkRootSyncDrift:
    name: checkRootSyncDrift
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: ${{ runner.temp }}/cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x "$RUNNER_TEMP/cuenv-bootstrap/cuenv"
        echo "$RUNNER_TEMP/cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: checkRootSyncDrift
      run: cuenv task checkRootSyncDrift --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  checkSwitchableAlternativeProgram:
    name: checkSwitchableAlternativeProgram
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: ${{ runner.temp }}/cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x "$RUNNER_TEMP/cuenv-bootstrap/cuenv"
        echo "$RUNNER_TEMP/cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: checkSwitchableAlternativeProgram
      run: cuenv task checkSwitchableAlternativeProgram --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  checkXmppClientFfiBindings:
    name: checkXmppClientFfiBindings
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: ${{ runner.temp }}/cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x "$RUNNER_TEMP/cuenv-bootstrap/cuenv"
        echo "$RUNNER_TEMP/cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: checkXmppClientFfiBindings
      run: cuenv task checkXmppClientFfiBindings --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  nixBuildCi:
    name: nixBuildCi
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: nixBuildCi
      run: nix build --print-build-logs ../#checks.x86_64-linux.waddle-server-ci-build
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  nixBuildExtensionModules:
    name: nixBuildExtensionModules
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: nixBuildExtensionModules
      run: nix build --print-build-logs ../#checks.x86_64-linux.waddle-server-extension-modules
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  nixClippy:
    name: nixClippy
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: nixClippy
      run: nix build --print-build-logs ../#checks.x86_64-linux.waddle-server-clippy
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  nixDoctest:
    name: nixDoctest
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: nixDoctest
      run: nix build --print-build-logs ../#checks.x86_64-linux.waddle-server-doctest
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  nixFmt:
    name: nixFmt
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: nixFmt
      run: nix build --print-build-logs ../#checks.x86_64-linux.waddle-server-fmt
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  nixTest:
    name: nixTest
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: nixTest
      run: nix build --print-build-logs ../#checks.x86_64-linux.waddle-server-test
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  renderDeployment:
    name: renderDeployment
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: ${{ runner.temp }}/cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x "$RUNNER_TEMP/cuenv-bootstrap/cuenv"
        echo "$RUNNER_TEMP/cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: renderDeployment
      run: cuenv task renderDeployment --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  verifyCuenvBootstrap:
    name: verifyCuenvBootstrap
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: verifyCuenvBootstrap
      run: nix build --print-build-logs --accept-flake-config ../#cuenv-source-coupling ../#cuenv-strict-discovery ../#cuenv-waddle-discovery
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
