# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: waddle-server-xmppCompliancePullRequest
on:
  pull_request:
    paths:
    - .github/workflows/waddle-server-xmppcompliancepullrequest.yml
    - cue.mod/**
    - server/../flake.lock
    - server/../flake.nix
    - server/Cargo.lock
    - server/Cargo.toml
    - server/crates/**
    - server/env.cue
    - server/schema/**
    - server/scripts/check-nix-cache-keys.sh
    - server/wit/**
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
  id-token: write
jobs:
  nixWarmServerDeps:
    name: nixWarmServerDeps
    runs-on: namespace-profile-linux-x86
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        fetch-depth: 2
    - name: Set up Namespace Nix cache
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Install Determinate Nix
      uses: DeterminateSystems/determinate-nix-action@92ffb5400c3776307a27a1727d7e2ac3dcd9f844
      with:
        extra-conf: accept-flake-config = true
    - name: nixWarmServerDeps
      run: nix build ../#waddle-server-deps ../#waddle-server-workspace-deps ../#waddle-server-workspace-all-features-deps ../#waddle-server-xmpp-deps ../#waddle-server-test-deps -L --accept-flake-config
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  nixXmppCueE2e:
    name: nixXmppCueE2e
    runs-on: namespace-profile-linux-x86
    needs:
    - nixWarmServerDeps
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        fetch-depth: 2
    - name: Set up Namespace Nix cache
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Install Determinate Nix
      uses: DeterminateSystems/determinate-nix-action@92ffb5400c3776307a27a1727d7e2ac3dcd9f844
      with:
        extra-conf: accept-flake-config = true
    - name: nixXmppCueE2e
      run: nix build ../#checks.x86_64-linux.waddle-server-xmpp-cue-e2e -L --accept-flake-config
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  nixXmppServerTests:
    name: nixXmppServerTests
    runs-on: namespace-profile-linux-x86
    needs:
    - nixWarmServerDeps
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        fetch-depth: 2
    - name: Set up Namespace Nix cache
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Install Determinate Nix
      uses: DeterminateSystems/determinate-nix-action@92ffb5400c3776307a27a1727d7e2ac3dcd9f844
      with:
        extra-conf: accept-flake-config = true
    - name: nixXmppServerTests
      run: nix build ../#checks.x86_64-linux.waddle-server-xmpp-server-tests -L --accept-flake-config
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  nixXmppUnitTests:
    name: nixXmppUnitTests
    runs-on: namespace-profile-linux-x86
    needs:
    - nixWarmServerDeps
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        fetch-depth: 2
    - name: Set up Namespace Nix cache
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Install Determinate Nix
      uses: DeterminateSystems/determinate-nix-action@92ffb5400c3776307a27a1727d7e2ac3dcd9f844
      with:
        extra-conf: accept-flake-config = true
    - name: nixXmppUnitTests
      run: nix build ../#checks.x86_64-linux.waddle-server-xmpp-unit-tests -L --accept-flake-config
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  nixXmppXepIntegration:
    name: nixXmppXepIntegration
    runs-on: namespace-profile-linux-x86
    needs:
    - nixWarmServerDeps
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        fetch-depth: 2
    - name: Set up Namespace Nix cache
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Install Determinate Nix
      uses: DeterminateSystems/determinate-nix-action@92ffb5400c3776307a27a1727d7e2ac3dcd9f844
      with:
        extra-conf: accept-flake-config = true
    - name: nixXmppXepIntegration
      run: nix build ../#checks.x86_64-linux.waddle-server-xmpp-xep-integration -L --accept-flake-config
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
