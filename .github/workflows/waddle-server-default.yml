# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: waddle-server-default
on:
  push:
    branches:
    - main
    paths:
    - .github/workflows/waddle-server-*.yml
    - .github/workflows/waddle-server-default.yml
    - .gitignore
    - .gitignore/**
    - .rules.cue
    - .rules.cue/**
    - apps/apple/Waddle/RustClient/Generated/**
    - cue.mod/**
    - cuenv.lock
    - cuenv.lock/**
    - env.cue
    - env.cue/**
    - flake.lock
    - flake.lock/**
    - flake.nix
    - flake.nix/**
    - infrastructure/waddle.cloud/gitops/kustomization-infra-waddle-server.yaml
    - infrastructure/waddle.cloud/gitops/kustomization-infra-waddle-server.yaml/**
    - infrastructure/waddle.cloud/gitops/waddle-server-source.yaml
    - infrastructure/waddle.cloud/gitops/waddle-server-source.yaml/**
    - infrastructure/waddle.cloud/gitops/waddle-server/**
    - server/**/env.cue
    - server/Cargo.lock
    - server/Cargo.lock/**
    - server/Cargo.toml
    - server/Cargo.toml/**
    - server/charts/waddle-server/**
    - server/crates/**
    - server/deployment.cue
    - server/deployment.cue/**
    - server/env.cue
    - server/extensions/**
    - server/rust-toolchain.toml
    - server/rust-toolchain.toml/**
    - server/schema/**
    - server/scripts/**
    - server/wit/**
  workflow_dispatch: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: none
  packages: write
  id-token: write
jobs:
  build-cuenv:
    name: build.cuenv
    runs-on: namespace-profile-linux-x86
    timeout-minutes: 30
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Setup cuenv (release)
      run: |-
        arch="$(uname -m)"
        case "$arch" in
          x86_64|amd64) cuenv_asset="cuenv-linux-x64" ;;
          aarch64|arm64) cuenv_asset="cuenv-linux-arm64" ;;
          *) echo "Unsupported Linux architecture: $arch" >&2; exit 1 ;;
        esac
        cuenv_version="${CUENV_VERSION}"
        if [ "$cuenv_version" = "latest" ]; then
          cuenv_url="https://github.com/cuenv/cuenv/releases/latest/download/${cuenv_asset}"
        else
          cuenv_url="https://github.com/cuenv/cuenv/releases/download/${cuenv_version}/${cuenv_asset}"
        fi
        curl -sSL -o /usr/local/bin/cuenv "$cuenv_url" && chmod +x /usr/local/bin/cuenv && /usr/local/bin/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Upload cuenv
      uses: actions/upload-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: result/bin/cuenv
        if-no-files-found: error
  buildExtensionModules:
    name: buildExtensionModules
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    - fmt
    - clippy
    - test
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: .cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x .cuenv-bootstrap/cuenv
        echo "$PWD/.cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: buildExtensionModules
      run: cuenv task buildExtensionModules --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: buildExtensionModules-artifacts
        path: |-
          server/target/wasm32-wasip2/release/link_board.wasm
          server/target/wasm32-wasip2/release/ai_chatbot.wasm
          server/target/wasm32-wasip2/release/decision_polls.wasm
          server/target/wasm32-wasip2/release/github.wasm
        if-no-files-found: ignore
        include-hidden-files: true
  checkCiDrift:
    name: checkCiDrift
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: .cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x .cuenv-bootstrap/cuenv
        echo "$PWD/.cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: checkCiDrift
      run: cuenv task checkCiDrift --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  checkRootSyncDrift:
    name: checkRootSyncDrift
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: .cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x .cuenv-bootstrap/cuenv
        echo "$PWD/.cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: checkRootSyncDrift
      run: cuenv task checkRootSyncDrift --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  checkXmppClientFfiBindings:
    name: checkXmppClientFfiBindings
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: .cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x .cuenv-bootstrap/cuenv
        echo "$PWD/.cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: checkXmppClientFfiBindings
      run: cuenv task checkXmppClientFfiBindings --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  clippy:
    name: clippy
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: .cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x .cuenv-bootstrap/cuenv
        echo "$PWD/.cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: clippy
      run: cuenv task clippy --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  fmt:
    name: fmt
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: .cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x .cuenv-bootstrap/cuenv
        echo "$PWD/.cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: fmt
      run: cuenv task fmt --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  publishContainerImage:
    name: publishContainerImage
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    - fmt
    - clippy
    - test
    - renderDeployment
    - buildExtensionModules
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: .cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x .cuenv-bootstrap/cuenv
        echo "$PWD/.cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: publishContainerImage
      run: cuenv task publishContainerImage --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
        CI_GITHUB_ACTOR: cuenv:passthrough:GITHUB_ACTOR
        CI_GITHUB_REF_NAME: cuenv:passthrough:GITHUB_REF_NAME
        CI_GITHUB_REF_TYPE: cuenv:passthrough:GITHUB_REF_TYPE
        CI_GITHUB_TOKEN: cuenv:passthrough:GITHUB_TOKEN
        CUENV_ARCH: amd64
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: publishContainerImage-artifacts
        path: target/digests/**
        if-no-files-found: ignore
        include-hidden-files: true
  renderDeployment:
    name: renderDeployment
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: .cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x .cuenv-bootstrap/cuenv
        echo "$PWD/.cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: renderDeployment
      run: cuenv task renderDeployment --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
  test:
    name: test
    runs-on: namespace-profile-linux-x86
    needs:
    - build-cuenv
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Cache /nix on Namespace volume
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: nix
    - name: Hand /nix to the runner user
      run: sudo chown -R runner /nix
    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: accept-flake-config = true
    - name: Download cuenv
      uses: actions/download-artifact@v4
      with:
        name: cuenv-bootstrap-namespace-profile-linux-x86
        path: .cuenv-bootstrap
    - name: Add cuenv to PATH
      run: |-
        chmod +x .cuenv-bootstrap/cuenv
        echo "$PWD/.cuenv-bootstrap" >> "$GITHUB_PATH"
    - name: test
      run: cuenv task test --skip-dependencies
      working-directory: server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REF_TYPE: ${{ github.ref_type }}
        GITHUB_REF_NAME: ${{ github.ref_name }}
