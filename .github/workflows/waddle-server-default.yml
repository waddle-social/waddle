# Generated by cuenv - do not edit manually
# Regenerate with: cuenv ci --format github

name: waddle-server-default
on:
  push:
    branches:
    - main
    paths:
    - .github/workflows/waddle-server-default.yml
    - cue.mod/**
    - server/../infrastructure/waddle.cloud/gitops/**
    - server/.dockerignore
    - server/Cargo.lock
    - server/Cargo.toml
    - server/Containerfile
    - server/charts/**
    - server/crates/**
    - server/env.cue
    - server/schema/**
  workflow_dispatch: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
  packages: write
jobs:
  waddle-server-default:
    name: waddle-server-default
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Setup cuenv (release)
      run: curl -sSL -o /usr/local/bin/cuenv https://github.com/cuenv/cuenv/releases/latest/download/cuenv-linux-x64 && chmod +x /usr/local/bin/cuenv && /usr/local/bin/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: 'Run pipeline: default'
      run: cuenv ci --pipeline default --path server
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
