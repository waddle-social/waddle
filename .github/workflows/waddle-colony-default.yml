# Generated by cuenv - do not edit manually
# Regenerate with: cuenv ci --format github

name: waddle-colony-default
on:
  push:
    branches:
    - main
    paths:
    - .github/workflows/waddle-colony-default.yml
    - colony/../bun.lock
    - colony/../package.json
    - colony/astro.config.ts
    - colony/dist/**
    - colony/drizzle.config.ts
    - colony/drizzle/**
    - colony/env.cue
    - colony/package.json
    - colony/public/**
    - colony/schema/**
    - colony/src/**
    - colony/tsconfig.json
    - colony/wrangler.jsonc
    - cue.mod/**
  workflow_dispatch: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
  packages: write
jobs:
  waddle-colony-default:
    name: waddle-colony-default
    runs-on: ubuntu-latest
    environment: production
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Setup cuenv (release)
      run: curl -sSL -o /usr/local/bin/cuenv https://github.com/cuenv/cuenv/releases/latest/download/cuenv-linux-x64 && chmod +x /usr/local/bin/cuenv && /usr/local/bin/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: cuenv:contributor:bun.workspace.setup
      run: 'true'
    - name: Setup 1Password
      run: cuenv exec -- cuenv secrets setup onepassword
      env:
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    - name: Install Bun dependencies
      run: cuenv exec -- bun install --frozen-lockfile
    - name: 'Run pipeline: default'
      run: cuenv ci --pipeline default --path colony
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        CUENV_ENVIRONMENT: production
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
