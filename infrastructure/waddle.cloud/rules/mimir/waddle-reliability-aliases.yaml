« the alias recording rules file: 30 recording rules across waddle-aliases-sm / -push / -pending / -broadcast / -delivery / -dnd / -traffic groups, each `record: waddle_<family>_total` with `expr: sum by (job, instance[, reason]) (xmpp_<family>_total)`; traffic group records waddle_messages_total, waddle_messages_per_second (1m rate), waddle_connected_users from waddle_messages_delivered_total / xmpp_connections_active »
