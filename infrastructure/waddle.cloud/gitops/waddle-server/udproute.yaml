apiVersion: gateway.networking.k8s.io/v1alpha2
kind: UDPRoute
metadata:
  name: waddle-sfu
  namespace: waddle
spec:
  parentRefs:
    - name: waddle-gateway
      namespace: default
      sectionName: sfu-udp
  rules:
    - backendRefs:
        - name: waddle-server
          port: 10000
