apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: gateway-to-livekit
  namespace: livekit
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: livekit
  to:
    - group: ""
      kind: Service
