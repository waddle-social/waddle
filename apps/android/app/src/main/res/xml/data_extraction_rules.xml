<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ twin of allowBackup="false": session tokens and message
     snapshots must never leave the device via backup or transfer. Each
     backup domain is walked independently, so all five must be excluded —
     a root-only exclude would leave files/ (DataStore session token),
     databases/, and shared_prefs/ eligible for device-to-device
     migration. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
