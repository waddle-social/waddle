name: Waddle
options:
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: 5.0
    MARKETING_VERSION: 0.1.0
    CURRENT_PROJECT_VERSION: 1
    GENERATE_INFOPLIST_FILE: YES
    ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
    FRAMEWORK_SEARCH_PATHS:
      - $(inherited)
      - $(SRCROOT)/Generated
targets:
  Waddle-iOS:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: Waddle
        excludes:
          - "**/*.md"
    dependencies:
      - framework: $(SRCROOT)/Generated/WaddleXmppClientFFI.xcframework
        embed: false
        link: true
    preBuildScripts:
      - name: Sync Waddle Brand Assets
        script: |
          set -euo pipefail
          /usr/bin/env -u SDKROOT -u SDK_NAME -u EFFECTIVE_PLATFORM_NAME -u PLATFORM_NAME -u ARCHS -u CURRENT_ARCH xcrun --sdk macosx swift "$SRCROOT/scripts/sync_brand_assets.swift"
    settings:
      base:
        PRODUCT_NAME: Waddle-iOS
        PRODUCT_BUNDLE_IDENTIFIER: p4x.waddle.social
        INFOPLIST_KEY_CFBundleDisplayName: Waddle
        INFOPLIST_KEY_CFBundleIconName: AppIcon
        INFOPLIST_KEY_UILaunchStoryboardName: LaunchScreen
        INFOPLIST_KEY_UISupportedInterfaceOrientations: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations~ipad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTS_MACCATALYST: NO
        SWIFT_OBJC_BRIDGING_HEADER: "Waddle/Waddle-Bridging-Header.h"
        HEADER_SEARCH_PATHS: "$(inherited) $(SRCROOT)/Generated/WaddleXmppClientFFI.xcframework/*/Headers $(SRCROOT)/Waddle/RustClient"
        MODULEMAP_FILE: "$(SRCROOT)/Waddle/RustClient/module.modulemap"
  Waddle-macOS:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: Waddle
        excludes:
          - "**/*.md"
          - LaunchScreen.storyboard
    dependencies:
      - framework: $(SRCROOT)/Generated/WaddleXmppClientFFI.xcframework
        embed: false
        link: true
    preBuildScripts:
      - name: Sync Waddle Brand Assets
        script: |
          set -euo pipefail
          /usr/bin/env -u SDKROOT -u SDK_NAME -u EFFECTIVE_PLATFORM_NAME -u PLATFORM_NAME -u ARCHS -u CURRENT_ARCH xcrun --sdk macosx swift "$SRCROOT/scripts/sync_brand_assets.swift"
    settings:
      base:
        PRODUCT_NAME: Waddle-macOS
        PRODUCT_BUNDLE_IDENTIFIER: p4x.waddle.social
        INFOPLIST_KEY_CFBundleDisplayName: Waddle
        INFOPLIST_KEY_CFBundleIconName: AppIcon
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.social-networking
        SWIFT_OBJC_BRIDGING_HEADER: "Waddle/Waddle-Bridging-Header.h"
        HEADER_SEARCH_PATHS: "$(inherited) $(SRCROOT)/Generated/WaddleXmppClientFFI.xcframework/*/Headers $(SRCROOT)/Waddle/RustClient"
        MODULEMAP_FILE: "$(SRCROOT)/Waddle/RustClient/module.modulemap"
schemes:
  Waddle:
    build:
      targets:
        Waddle-iOS: all
        Waddle-macOS: all
  Waddle-iOS:
    build:
      targets:
        Waddle-iOS: all
  Waddle-macOS:
    build:
      targets:
        Waddle-macOS: all
