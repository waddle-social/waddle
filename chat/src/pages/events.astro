---
import AppLayout from "@/layouts/AppLayout.astro";
import RoutePageShell from "@/components/pages/RoutePageShell.vue";
import { routes } from "@/router";

const initialMatch = routes.events.match();
---

<AppLayout initialMatch={initialMatch}>
  <RoutePageShell client:only="vue" routeId="events" initialMatch={initialMatch} />
</AppLayout>
