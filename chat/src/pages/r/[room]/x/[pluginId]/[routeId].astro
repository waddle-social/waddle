---
import AppLayout from "@/layouts/AppLayout.astro";
import RoutePageShell from "@/components/pages/RoutePageShell.vue";
import { routes } from "@/router";

const { room, pluginId, routeId } = Astro.params;
const initialMatch = routes.channelExtension.match({
  params: {
    channelId: room ?? "",
    pluginId: pluginId ?? "",
    routeId: routeId ?? "",
  },
});
---

<AppLayout initialMatch={initialMatch}>
  <RoutePageShell client:only="vue" routeId="channelExtension" initialMatch={initialMatch} />
</AppLayout>
