---
import AppLayout from "@/layouts/AppLayout.astro";
import RoutePageShell from "@/components/pages/RoutePageShell.vue";
import { routes } from "@/router";

const initialMatch = routes.feed.match();
---

<AppLayout initialMatch={initialMatch}>
  <RoutePageShell client:only="vue" routeId="feed" initialMatch={initialMatch} />
</AppLayout>
