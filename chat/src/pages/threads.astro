---
import AppLayout from "@/layouts/AppLayout.astro";
import ThreadsPage from "@/components/pages/ThreadsPage.vue";
import { routes } from "@/router";

const initialMatch = routes.threads.match();
---

<AppLayout initialMatch={initialMatch}>
  <ThreadsPage client:only="vue" initialMatch={initialMatch} />
</AppLayout>
